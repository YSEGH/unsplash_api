import { Button } from "@mui/material";
import React from "react";

type Props = {};

const DownloadOneButton = (props: Props) => {
  return <Button>DownloadOneButton</Button>;
};

export default DownloadOneButton;
